# 品牌站外流量监测 — 使用说明

## 环境要求
- 已安装 Node.js 18 或更高版本（https://nodejs.org/）

## 本地启动
1. 解压本压缩包
2. 进入解压后的目录（含 `package.json` 的那一层）
3. 终端执行：

```bash
npm install
npm start
```

4. 浏览器打开：**http://127.0.0.1:3000**（根路径即可，无需 `/index.html`）
5. 输入关键词（如 `Mammotion,LUBA,YUKA`），点击 **「开始监控」**
6. 拉取站外数据 + 豆包归类通常需要 **10–30 秒**，请稍候

## Railway / GitHub 部署
1. 代码推到 GitHub 后，Railway 选 **项目根目录**（不要只选 `backend`）
2. Start Command：`npm start`
3. 在 Railway **Variables** 里配置 `.env` 中所有 Key（`LLM_API_KEY`、`LLM_MODEL`、`LLM_BASE_URL`、`REDDITDATE_API_KEY` 等）
4. 部署完成后访问 Railway 生成的域名根路径即可

## 已对接说明
- **大模型归类（豆包 / 火山方舟）**：`.env` 已示例配置
- **Google News RSS**：无需 Key，默认可拉新闻
- **YouTube / Google 自定义搜索（Ins、TikTok、Deal 站等）**：代码已接，需客户自备 Key 后写入 `.env`
- **RedditDate**：已配置示例 Key，可按需更换
- **大模型 API 调用费用由客户承担**

## 图表说明（2026-08-17 更新）
- **渠道分布**：按实际渠道（新闻、Reddit 等）展示；无渠道数据时按四类（PR/联盟/红人/社区）展示
- **情感分析**：根据标题/摘要做轻量情感推断，显示正向占比及正/中/负条数

## 常见问题
- **页面打不开**：确认服务已启动；本地用 `127.0.0.1:3000`；Railway 检查 Variables 与部署日志
- **一直加载**：正常约 10–30 秒；超过 1 分钟看终端/ Railway 日志
- **换豆包 Key**：改 `.env` 中 `LLM_API_KEY`、`LLM_MODEL`（`ep-xxxx`），重启服务
