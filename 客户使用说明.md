# 品牌站外流量监测 — 使用说明

## 环境要求
- 已安装 Node.js 18 或更高版本（https://nodejs.org/）

## 启动步骤
1. 解压本压缩包
2. 进入解压后的目录（含 `package.json` 的那一层）
3. 打开终端，执行：

```bash
npm install
npm start
```

4. 浏览器打开：**http://127.0.0.1:3000**
5. 输入关键词（如 `Mammotion,LUBA,YUKA`），点击 **「开始监控」**
6. 拉取站外数据 + 豆包归类通常需要 **10–30 秒**，请稍候

## 已对接说明
- **大模型归类（豆包 / 火山方舟）**：已在 `.env` 中配置（`LLM_API_KEY` / `LLM_MODEL` / `LLM_BASE_URL`）
- **RedditDate**：已配置客户提供的 Key
- 其他数据源（YouTube、Google CSE 等）未配 Key 时会自动跳过，不影响主流程
- **大模型 API 调用费用由客户承担**

## 本期功能对照
1. 功能可启动使用  
2. 有 Key 拉真实数据；无 Key 跳过/降级  
3. 结果归为四类：PR / 联盟 / 红人内容 / 社区舆情  
4. 地区筛选；地区可由大模型归类  
5. 四类优先走大模型归类（超时则回退规则，避免卡死）  
6. Key 客户自备，费用客户承担  

## 常见问题
- **页面打不开**：确认终端里服务已启动，用 `127.0.0.1:3000`，不要用被 VPN 劫持的 localhost  
- **一直加载**：属正常等待；超过 1 分钟仍无结果，看终端报错  
- **换自己的豆包 Key**：改项目根目录 `.env` 里的 `LLM_API_KEY` 和 `LLM_MODEL`（推理接入点 `ep-xxxx`），然后重启 `npm start`
